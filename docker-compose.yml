version: '3'
services:
  server:
    build: .
    container_name: tarbean
    environment:
      - VIRTUAL_HOST=tarbean.suilabs.com
      - LETSENCRYPT_HOST=tarbean.suilabs.com
      - LETSENCRYPT_EMAIL=borja.arias.upc@gmail.com
    ports:
      - "3001:3000"
    volumes:
      - ./public:/usr/src/app/public
  test:
    build:
      context: .
      dockerfile: ./test/Dockerfile


networks:
  default:
    external:
      name: webplatform_suilabs